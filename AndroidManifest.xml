<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  android:versionCode="6"
  android:versionName="open_0.1">
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
  <uses-permission android:name="com.android.vending.BILLING" />
  <uses-permission android:name="android.permission.GET_ACCOUNTS" />
  <uses-permission android:name="android.permission.USE_CREDENTIALS" />
	    
  <application android:label="@string/app_name" 
    android:icon="@mipmap/ic_launcher"
    android:name="CatTailApplication"
    >
    <!-- The bundled, pre-modern Google Analytics and ACRA jars still link
         Apache HTTP.  API 36 exposes it only as this optional compatibility
         shared library; the later Kotlin dependency migration removes it. -->
    <uses-library android:name="org.apache.http.legacy" android:required="false" />
    <activity android:name="Nanidroid"
      android:label="@string/app_name"
      android:launchMode="singleTop"
      android:exported="true"
      android:theme="@style/NanidroidTheme">
      <intent-filter>
	<action android:name="android.intent.action.MAIN" />
	<category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter >
	<action android:name="android.intent.action.VIEW" />
	<category android:name="android.intent.category.DEFAULT" />
	<data android:scheme="https" />
	<data android:pathPattern=".*\\.nar" />
	<data android:pathPattern=".*\\.zip" />
      </intent-filter>

    </activity>
    <service android:name=".NanidroidService" android:exported="false"
      android:foregroundServiceType="dataSync" />
    <activity android:name=".Preferences" android:exported="false" android:theme="@style/NanidroidTheme" />

  </application>
  
</manifest>
